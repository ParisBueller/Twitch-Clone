//history was installed with react-router-dom
import createHistory from 'history/createBrowserHistory';
//creates a router history object that 
//we can access anywhere inside project
export default createHistory();